ngDescribe({
    name: 'Test ComponentConfigService',
    modules: 'app',
    inject: 'ComponentConfigService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
